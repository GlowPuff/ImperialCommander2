# ORDEM DE TRADUÇÃO: Star Wars Imperial Assault

**Contexto e Referências:**
Atue conforme as diretrizes estabelecidas no arquivo `SYSTEM.md`. Utilize os seguintes arquivos anexos como sua base de conhecimento obrigatória:
1. `dicionario.json` (Vocabulário e Regras de Preservação).
2. `manual.txt` (Contexto Gramatical e Lógica).

**Arquivo Alvo:**
Traduza o arquivo JSON anexo: `[INSIRA_NOME_DO_SEU_ARQUIVO.json]`

**DIRETRIZES DE SEGURANÇA JSON (Risco Zero):**
Para garantir a integridade do código, você deve seguir estas restrições técnicas:

1.  **Chaves Permitidas:** Traduza APENAS os valores associados às seguintes chaves. Ignore todo o resto:
    * `"name"`
    * `"text"`
    * `"flavor"`
    * `"ability"`
    * `"subname"`
    * `"description"`
    * `"eventText"`
    * `"missionText"`
    * *(Adicione ou remova chaves aqui conforme o seu arquivo específico)*

2.  **Estrutura:** Não altere a estrutura do JSON. Não traduza nomes de chaves (ex: mantenha `"type":`, `"id":`, `"groups":`).

3.  **Preservação:**
    * Verifique cada termo contra a `lista_de_preservacao` no arquivo de referência ANTES de traduzir.
    * Mantenha intactas todas as tags de formatação como `{amount}`, `<red>`, `\n`.

**Saída Esperada:**
Forneça o arquivo JSON completo e traduzido. Se o arquivo for muito longo, faça a tradução em blocos lógicos, mas mantenha a validade do JSON em cada bloco.

Comece a análise e tradução agora.